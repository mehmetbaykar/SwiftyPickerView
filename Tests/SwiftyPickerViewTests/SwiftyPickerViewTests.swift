import XCTest
@testable import SwiftyPickerView

final class SwiftyPickerViewTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(SwiftyPickerView().text, "Hello, World!")
    }
}
